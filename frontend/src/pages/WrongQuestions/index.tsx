import { useEffect, useState } from 'react'; import { CheckCircle2, Sparkles, XCircle } from 'lucide-react'; import { api } from '../../api/client'; import { useAppStore } from '../../stores/useAppStore'; import type { Mistake } from '../../types';
export default function WrongQuestions(){const subject=useAppStore((s)=>s.currentSubject)!;const[items,setItems]=useState<Mistake[]>([]);useEffect(()=>{api.mistakes(subject.id).then(setItems)},[subject.id]);return <div className="h-full overflow-y-auto p-6 md:p-8"><header className="mb-7"><h1 className="text-3xl font-bold">智能错题本</h1><p className="mt-2 text-slate-500">集中复盘 {subject.name} 的错误与 AI 诊断。</p></header><div className="space-y-5">{items.map((item)=><article key={item.id} className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="border-b bg-slate-50 px-5 py-3 text-sm"><b className="rounded bg-blue-100 px-2 py-1 text-blue-700">{item.topic}</b><span className="ml-3 text-slate-400">{item.createdAt}</span></div><div className="p-6"><h2 className="text-lg font-bold"><span className="mr-2 text-rose-500">Q.</span>{item.question}</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-rose-100 bg-rose-50/40 p-4"><div className="mb-2 flex items-center text-xs font-bold text-rose-500"><XCircle size={15} className="mr-1"/>你的答案</div>{item.myAnswer}</div><div className="rounded-xl border border-emerald-100 bg-emerald-50/40 p-4"><div className="mb-2 flex items-center text-xs font-bold text-emerald-600"><CheckCircle2 size={15} className="mr-1"/>参考答案</div>{item.correctAnswer}</div></div><div className="mt-4 rounded-xl border border-blue-100 bg-blue-50/50 p-4"><div className="mb-2 flex items-center font-bold text-blue-800"><Sparkles size={16} className="mr-2"/>AI 诊断</div><p className="text-sm leading-6 text-slate-700">{item.reason}</p></div></div></article>)}</div></div>}

